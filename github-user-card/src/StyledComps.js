import { makeStyles } from '@material-ui/core'

export const userCardStyles = makeStyles({
    bigAvatar: {
        margin: 10,
        width: 125,
        height: 125,
        alignSelf: 'center',
    },
})